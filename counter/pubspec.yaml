name: counter
description: A Flutter counter app using Provider.
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1

  # The following adds the Cupertino Icons font to your application.
  cupertino_icons: ^1.0.2

flutter:
  uses-material-design: true